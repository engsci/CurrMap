- semantic_form_for @collection do |form|
  = form.inputs do 
    .column
      = form.inputs :name
  
    .column
      = render :partial => 'partials/form_relation', :locals => {:form => form, :object => @collection, :relation => 'course_instances'}
      = render :partial => 'partials/form_relation', :locals => {:form => form, :object => @collection, :relation => 'courses'}
      = render :partial => 'partials/form_relation', :locals => {:form => form, :object => @collection, :relation => 'parent_collections'}
      = render :partial => 'partials/form_relation', :locals => {:form => form, :object => @collection, :relation => 'child_collections'}
      
  - form.buttons do
    = form.commit_button
    or 
    =link_to "Go Back", @collection